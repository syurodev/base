---
title: Implementation status
description: Trạng thái implementation đã kiểm chứng của DevKit.
---

# Implementation status

Snapshot được kiểm tra từ checkout desktop `dev-kit-app/`. Status có
thể thay đổi theo commit; kiến thức kiến trúc ổn định nằm ở các trang khác.

## Status vocabulary

| Status | Meaning |
|---|---|
| Implemented | Có runtime code và test/build evidence phù hợp. |
| Partial | Có runtime code nhưng còn thiếu capability hoặc production behavior quan trọng. |
| Design only | Có architecture/spec nhưng chưa có runtime package/flow. |
| Not started | Chưa có implementation đáng kể. |

## Module matrix

| Module | Status | Evidence | Remaining scope |
|---|---|---|---|
| Crypto envelope | Implemented | `internal/crypto/envelope.go`, `internal/crypto/aead.go`; tests: `internal/crypto/envelope_test.go`, `internal/crypto/aead_test.go`; V2-only `SealV2`/`OpenWithAD` with canonical AAD and explicit V1 rejection. | Future envelope changes require an explicit versioned migration decision; no V1 migration tool is in scope. |
| Vault | Implemented | `internal/vault/service.go`, `internal/vault/keyringstore.go`, `internal/vault/sqlkeyringstore.go`; tests: `internal/vault/service_test.go`, `internal/vault/keyringstore_test.go`, `internal/vault/sqlkeyringstore_test.go`; UI: `frontend/src/workbench/VaultGate.tsx` (`VaultShell`, `StepProgress`, `RecoveryKeyField`, `LoadingStatus`, `SecurityNotice`), lock timeouts and vault reset in `SettingsView`. | Hoàn thiện UX và policy cho recovery / strength hints nếu cần. |
| Notes | Implemented | `internal/notes/{notes,folders,tags}.go` (thin adapter trên `vaultitem.Store`, `note` + `note-folder` record type, live tag/color inheritance); tests: `internal/notes/{notes,folders,tags}_test.go`; UI: `frontend/src/modules/notes/{NotesView,NotesDashboard,NoteEditor,NoteCard,ColorPicker}.tsx` (dashboard → Plate editor, in-place edit via autosave, one-level folders, freeform tags, 9-màu per-note color). Editor layout: cột viết căn giữa `max-w-[720px]`, metadata (folder/color/tag) chuyển vào `NoteMetaPanel` trong shared split panel (ADR-011), toggle qua nút title-bar thay vì header cục bộ. | Title vẫn là metadata plaintext; cần encrypt title nếu titles ever sync. Folder one-level only; note color giới hạn palette 9 màu dùng chung với folder. |
| Tools | Implemented | `internal/tools/hash.go`, `internal/commands/registry.go`; test: `internal/tools/hash_test.go`; UI: `frontend/src/modules/tools/ToolsView.tsx`. | Mở rộng thêm tools khi product cần. |
| SSH | Implemented | `internal/ssh/{ssh,hostkeys,forwarding,active}.go`; create bắt buộc `env` hợp lệ (`EnvLookup`); bridge `internal/bridge/ssh.go` + active profile; UI `frontend/src/modules/ssh/SSHView.tsx` (dropdown env). | Forward runtime-only, loopback-only. |
| Database | Implemented | `internal/database/*.go` (driver abstraction, 2 engine, tunnel, safe-mode, history/saved, schema cache, FK, live activity, query plan); create bắt buộc `env` hợp lệ; `Driver`/`Conn` abstraction, Postgres + MySQL/MariaDB; safe-mode `read_only`; SSH tunnel qua `ssh_profile_id`; query history cục bộ + saved query đồng bộ; export qua `DBSaveExportFile`. Bridge `internal/bridge/database.go`; UI `frontend/src/modules/database/DatabaseView.tsx`. | Engine không sửa được sau khi tạo. Tunnel chỉ dùng được với SSH key không passphrase. Query plan chỉ Postgres; MySQL không trả `BlockedBy`. UI cho activity/ERD/EXPLAIN vẫn là thiết kế Figma. |
| Kafka | Implemented | `internal/kafka/{kafka,client,broker,active}.go`; create bắt buộc `env` hợp lệ; bridge `internal/bridge/kafka.go` + active cluster; UI `frontend/src/modules/kafka/KafkaView.tsx` (dropdown env). franz-go client (pure Go). | Schema Registry/Avro out of scope v1. |
| Code Graph (Java) | Implemented | `analyzer/` CLI (JavaParser + JavaSymbolSolver), `internal/codegraph/{codegraph,service,plan,indexer}.go`; tests: `internal/codegraph/service_test.go`, `analyzer/src/test/...`; MCP: `internal/mcp/tools_codegraph*.go`; bridge: `internal/bridge/codegraph.go`; frontend: `frontend/src/modules/codegraph/` (`CodeGraphView`, `CodeGraphSidebar`, `FileTree`, `BlastRadiusPanel`, `graph/`, `inspector/`, `plan/`, `terminal/`) + CodeMirror 6 editor primitive (`frontend/src/components/code-editor/CodeEditor.tsx`). | Đa ngôn ngữ (chỉ Java v1). 1 project active per session. Giao comment vào phiên agent đang chạy chưa làm — xem `internal/codegraph/comments.go`. |
| Environments | Implemented | `internal/environments/{environments,memory,sqlrepository}.go`; migration m12 `env_tags` seed `dev`/`stg`/`prod`; `RequiresConfirmation` fail-closed; bridge `Env*`; UI Settings; `DeleteTag` chặn tag-in-use qua `ProfileEnvUsage` (`internal/app/envusage.go` quét ssh/db/kafka profiles). | — |
| Vault item store | Implemented | `internal/vaultitem/vaultitem.go`, `internal/vaultitem/memory.go`, `internal/vaultitem/sqlrepository.go`, `internal/vaultitem/store.go`; tests: `internal/vaultitem/memory_test.go`, `internal/vaultitem/sqlrepository_test.go`, `internal/vaultitem/store_test.go`; bridge search: `internal/bridge/search.go`, `internal/bridge/search_test.go`. | Search hiện là substring trên metadata JSON; chưa có index trên cột `type`. |
| Capability Gateway | Implemented | `internal/capabilities/gateway.go`, `internal/permissions/policy.go`, `internal/audit/events.go`, `internal/bridge/gateway.go`; tests: `internal/capabilities/gateway_test.go`, `internal/bridge/gateway_test.go`; grants gồm `builtin.search`/`vault:search`/`item:search` cho cross-type search. | Cần mở rộng policy khi external callers / plugin surface xuất hiện. |
| ModuleManifest / Internal Extension API | Implemented | `internal/modules/registry.go` và `internal/app/app.go` định nghĩa/đăng ký manifest; `internal/bridge/app_service.go` expose public metadata; frontend manifests, parity validation và minimal functional UX ở `frontend/src/modules/{registry.ts,*/manifest.ts}` và `frontend/src/workbench/{Workbench.tsx,CommandPalette.tsx}` + `frontend/src/workbench/dock/{Dock,DockItem}.tsx`. Shared cross-module UI primitives (ADR-011): `frontend/src/stores/moduleActions.ts` (title-bar action registry) + `frontend/src/components/ui/split-panel.tsx` (content-squeezing side panel). Tab strip (ADR-012, Phase 1 module-level tabs): `frontend/src/stores/workbenchTabs.ts` + `frontend/src/workbench/tabs/TabStrip.tsx`, reorder isolation memoization `frontend/src/workbench/workbenchViewMemo.ts` (tests: `frontend/tests/workbench-reorder.test.ts`). | External plugin runtime chưa nằm trong scope. Per-note / per-session tabs (Notes, Agent Hub) và live tab status qua `agenthub:sessionUpdate` chưa implement. |
| Sync / Replication client | Implemented | Phase 3B transport/pull/apply, Phase 3C local mutation flow và **Git-style conflict resolve** đã có. `internal/auth` dùng Authorization Code + PKCE S256 qua system browser + loopback callback, refresh-token rotation trong OS keychain và access token chỉ in-memory. `syncclient.ConnectWithTokenSource` lấy token mới trước request; `SyncConnect` pin Gateway `https://api.synx.io.vn`. Enrollment token gửi trên sync session handshake. Deployed matrix G1–G4 pass qua `cmd/synce2e/` trên Compose stack. Evidence: `internal/auth/{service,keyring_store}.go`, `internal/syncclient/{service,connect,syncnow}.go`, `internal/bridge/sync.go`, `cmd/synce2e/`; tests `internal/auth/service_test.go`, `internal/bridge/sync_test.go`, `go test ./cmd/synce2e/ -count=1 -timeout 15m`. | JetBrains 3-pane merge UI → Phase 5 Workbench UI. |
| Sync SaaS backend + auth gateway | Implemented | Companion checkout `dev-kit-backend/` (branch `feat/phase-b-device-revoke`, `2060f39`): Spring Boot `session/push/pull`, PostgreSQL Liquibase schema, account/device registration + revoke guard, atomic arbitration/audit. `gateway/` validate Keycloak JWT, strip identity/proxy headers, ký JWT nội bộ ≤45s, publish JWKS, `GET /v1/desktop/config`, Redis denylist early reject (fail-open). **BE Phase B**: `GET/POST /v1/sync/devices` list+revoke, last-ACTIVE `409`, Redis `sync:revoked-device:{sub}:{deviceId}` TTL 60s. Desktop bridge tối thiểu (`02afaaa`). Evidence: `./gradlew :test --tests com.synx.devkit.e2e.SyncApiIT`, `./gradlew :gateway:test`, desktop `go test ./internal/sync ./internal/bridge ./internal/app ./internal/arch`. G4 rewritten to API revoke; re-run `go test ./cmd/synce2e/ -run TestG4RevokedDeviceRejected` against Compose when verifying deployed matrix. | UI device management, key-rotation runbook, retention/backup vẫn later. Spec: `docs/superpowers/specs/2026-08-11-be-phase-b-device-revoke-design.md`. |
| MCP | Implemented | `internal/mcp` (socket host + tools), `cmd/devkit-mcp` (stdio proxy + install), `internal/interaction` (selection/confirmation; headless fail-fast); install `claude-code`/`codex` × `global`/`project`; env-wins over `allow_always`. Tool sets: `notes.*`, `db.*`, `ssh.*`, `kafka.*`, `codegraph.*`, `agenthub.*`. | Resources / Streamable HTTP deferred. Not production-verified vs real Claude Code in CI — desktop smoke recommended. |
| Agent Hub | Implemented | Phase 4 v1 **Claude-only closed**: `internal/acp`, `internal/agenthub` (mock + Claude), bridge + ModuleManifest, frontend `frontend/src/modules/agenthub/`, MCP `agenthub.*`, Claude spawn `sandbox.DefaultRunner().StartPiped` (fail-closed). Docs: [`/dev-kit/agent-hub`](/dev-kit/agent-hub). Tests: `go test ./internal/acp/ ./internal/agenthub/ ./internal/bridge/ ./internal/mcp/`. | Codex/Gemini `VendorAdapter`s deferred (post–Phase 5 UI ok); production CI smoke vs real Claude Code; UX polish → Phase 5. |
| Agent sandbox (`internal/sandbox`) | Implemented | Leaf package: Linux `bwrap`, Landlock helper, macOS Seatbelt; fail-closed network/bind policy; arch zero in-module imports. **`Start`** = capture-and-wait; **`StartPiped`** = interactive ACP stdio (used by Claude adapter). Docs: [`/dev-kit/sandbox`](/dev-kit/sandbox). Tests: `go test ./internal/sandbox/ ./internal/arch/`. Merged desktop PR #22 + StartPiped on Agent Hub branch. | Domain egress proxy; Windows; optional Landlock-in-Start/StartPiped. |
| External plugins | Design only | Centralized architecture sources: `/dev-kit/plugins`, `/dev-kit/repo-structure`; reproducible absence check: `test ! -d ./internal/plugins`. **Roadmap Phase 6** (sau Workbench UI). | Build plugin SDK, loader, isolation, permissioning, and lifecycle. |
| Marketplace | Design only | Centralized architecture sources: `/dev-kit/plugins`, `/dev-kit/core-foundation`; reproducible absence check: `test ! -d ./internal/marketplace`. **Roadmap Phase 7**. | Signing, integrity checks, rollback, and controlled distribution model. |
| Vault item repository | Implemented | Unified store thay 3 store riêng: `internal/vaultitem/*` với bảng `vault_items`; notes/database/ssh là thin adapter; cross-type search qua `VaultSearch`. Xem dòng "Vault item store" ở trên. | App hiện greenfield, chưa có legacy data nên migration `m6` không cần backfill. Chỉ thêm migration/backfill trước khi hỗ trợ upgrade từ bản đã có dữ liệu; vẫn cần migration/re-encrypt khi đổi envelope hoặc rotate `key_version`, và index khi search lớn. |

## Verified snapshot

**Đây là snapshot chuẩn của repo.** Trang khác không lặp lại branch/commit/ngày
mà chỉ giữ evidence riêng của mình và trỏ về đây:
[Security controls](/dev-kit/security-controls#evidence-pass-2026-08-10),
[Operations](/dev-kit/operations#evidence-pass-2026-08-01),
[Architecture decisions](/dev-kit/architecture-decisions#evidence-pass-2026-07-24).

- Checked on: 2026-08-23 (CodeGraph plan flow + comments, right-panel split, shell-owned agent chat và terminal)
- DevKit checkout branch: `main`
- Đã làm trong pass này:
  - **Plan-first flow ra UI**: canvas plan với trạng thái từng node, duyệt plan, và relink FQN
    (`frontend/src/modules/codegraph/plan/`). Layer của plan node **suy từ tên** vì
    `LayerClassifier` đọc annotation Spring, thứ mà class chưa tồn tại không thể có.
  - **Comment trên plan node**: bảng `plan_node_comments`, thread trả kèm `getPlannedFlow`,
    MCP `codegraph.replyPlanNode`, popover neo vào node (`plan/PlanCommentsPopover.tsx`).
  - **`ListFields`** — thiếu nó thì `traceDataFlow` không gọi được, vì không API nào sinh ra
    được tên field.
  - **Cột phải chia hai slot**: slot trên của module, slot dưới của shell
    (`frontend/src/workbench/splitColumn.ts`, `components/ui/split-panel.tsx`).
  - **Hội thoại agent và terminal chuyển lên shell**. Cả hai từng nằm trong module, tức là
    rời module là mất — với chat còn mất cả subscription nên lượt trả lời của agent bị **rơi
    hẳn**, không phải chỉ bị ẩn.
  - **Event `codegraph:indexUpdated`**: re-index nền giờ báo cho UI, trước đây danh sách
    endpoint và trạng thái plan cứ âm thầm cũ đi.
  - **Terminal trong bottom slot** kèm tab, output PTY qua base64 tường minh cả hai chiều.
- Verification commands run:
  - `go test ./internal/...` → PASS (35 packages)
  - `go vet ./internal/...` → PASS
  - `cd frontend && npx tsc --noEmit` → PASS
  - `cd frontend && bun test` → PASS (86 tests)
  - `cd frontend && npx vite build` → PASS
- Chưa verify bằng tay ở pass này: mọi thứ trên chạy qua test và build, phần render thật do
  người dùng xác nhận trực tiếp (index tự cập nhật khi sửa file ngoài app; terminal ra prompt
  đúng thư mục project).

- Checked on: 2026-08-19 (CodeGraph implementation, Vault onboarding & recovery UX overhaul, Sync & conflict resolution hardening, Workbench tab reorder memoization)
- DevKit checkout branch: `main`
- DevKit checkout status: Phase 2–4 + CodeGraph `Implemented`. Vault UX overhaul `Implemented`. Agent Hub **Implemented** (Claude-only v1; Codex/Gemini Design). Sandbox **Implemented** including `StartPiped` and live-verified `NetworkHost` on macOS.
- Core hardening & features verified this pass:
  - CodeGraph (Java): JavaParser analyzer CLI (`analyzer/`), Go indexing service (`internal/codegraph/`), MCP tools (`internal/mcp/tools_codegraph*.go`), and Workbench UI (`frontend/src/modules/codegraph/` with FileTree, Flow Diagram, Graph View, and CodeMirror 6 editor primitive).
  - Vault & Auth UX: Redesigned onboarding & unlock flows with `VaultShell`, `StepProgress`, `RecoveryKeyField`, `LoadingStatus`, `SecurityNotice`; SettingsView vault reset and lock timeout controls; thread-safe derive+install serialization and atomic keyring file persistence.
  - Sync & Conflict Resolution: capture-any-divergence (version-asymmetric, dirty-local vs newer remote, delete-theirs with nullable snapshot m15), outbox failure dead-lettering, unified `sync_conflicts` read source, full 2-device mock server E2E test coverage (`TestE2EAsymmetricDivergenceConverges`, `TestE2ETrashVsEditUseTheirsConverges`, `TestE2ETrashVsEditUseOursResurrects`).
  - Workbench Tab Isolation: Tab reorder memoization (`workbenchViewMemo.ts`) preventing unnecessary component unmount/remount on drag-and-drop tab reordering, covered by unit tests (`workbench-reorder.test.ts`).
  - Sandbox & MCP Gate: `GateActiveMCP` returns pinned confirmed profile ID; Kafka connection deferred until post-confirmation; seatbelt profile passed via `-p` argument.
- Verification commands run (representative):
  - `go test ./...` → PASS (toàn bộ internal packages và cmd)
  - `go test -race ./internal/vault/ ./internal/app/` → PASS
  - `go test ./cmd/mocksyncserver/ -count=1` → PASS (gồm 3 E2E conflict mới)
  - `DEVKIT_SANDBOX_SMOKE=1 go test ./internal/sandbox/ -run TestSmokeSeatbeltNetworkHost` → PASS (live)
  - `cd frontend && bun test` → PASS (5/5 tests)
  - `cd frontend && bun run build` → PASS (performance budget OK)

Phase 2–4 + CodeGraph đều `Implemented`. Vault onboarding UX và Sync conflict resolution hoàn thiện. CodeGraph đã đủ plan flow, comment, data flow, blast radius và terminal. **Next:** giao comment vào phiên agent đang chạy (cần link plan→session, hiện chưa có gì lưu), rồi Phase 5 Workbench UI polish & plugin architecture (Phase 6).

## Companion backend snapshot

- Checked on: 2026-08-11
- Backend checkout: `dev-kit-backend/`,
  branch `feat/phase-b-device-revoke`, commit `2060f39`.
- Gateway milestone có Gradle subproject `gateway/`, Keycloak realm import,
  API/Gateway Docker images, desktop remote config (`GET /v1/desktop/config`),
  Redis denylist filter (fail-open), và private sync network trong Compose.
- BE Phase B device revoke: list/revoke API, enrollment cleanup, audit,
  Redis denylist TTL 60s; desktop bridge tối thiểu (`dev-kit-app` `02afaaa`).
- Verification đã chạy (this session + prior):
  - `./gradlew :test --tests com.synx.devkit.e2e.SyncApiIT` → PASS;
  - `./gradlew :gateway:test` → PASS (denylist filter);
  - desktop `go test ./internal/sync ./internal/bridge ./internal/app ./internal/arch` → PASS;
  - G4 API path updated; deployed matrix `TestG4RevokedDeviceRejected` not re-run
    this session — re-run against Compose when verifying.

## Current limitations

- SQLite storage is application-layer encrypted via vault envelopes and file
  permissions, not full-disk database encryption.
- Note titles stay plaintext local metadata for now.
- SSH covers profiles (required `env` tag), exact host-key trust, test connection,
  command execution, and loopback-only local/remote TCP forwarding. Forward
  sessions are runtime-only and stop on profile delete / app shutdown. MCP sticky
  active profile + env confirmation gate are Implemented.
- Database Postgres profiles cũng bắt buộc `env`; multi-engine vẫn design. MCP
  active profile + confirmation Implemented.
- Kafka profiles bắt buộc `env`; UI/bridge path Implemented (profiles, connect,
  topics, peek, consumer groups, publish). MCP tools + cluster-selection popup +
  env confirmation Implemented (headless fail-fast).
- Environments Settings quản lý tag; `DeleteTag` scan gồm `kafka-profile`;
  `RequiresConfirmation` fail-closed feeds MCP confirmation (env wins over
  `allow_always`).
- MCP: Implemented runtime (socket host, stdio proxy, tools, install for
  `claude-code`/`codex` × `global`/`project`). Resources / Streamable HTTP
  deferred. Not production-verified against a real Claude Code session in CI.
- Agent Hub is **Implemented** (Phase 4 Claude-only): ACP + service +
  mock/Claude + bridge/UI + `agenthub.*` MCP tools. Codex/Gemini adapters still
  Design (not required to close Phase 4). Not claimed production-verified against
  real Claude Code in CI.
- External plugin runtime is still design-only (**Phase 6**; marketplace
  **Phase 7** — after Workbench UI Phase 5).
- Sync client hiện có server-authoritative account session, runtime-only bearer,
  HTTPS transport (HTTP chỉ cho loopback), encrypted push/pull, transactional
  apply/cursor, replay journal, durable local mutation intents, capability-gated
  bridge, minimal Sync UI, và **Git-style conflict resolve** (list / capture
  theirs / resolve ours|theirs|`merge` notes-only). Local mutation, version và intent
  commit atomically; ACK cũ không được đánh dấu edit mới hơn là synced. Không
  có auto-winner. Companion backend đã có registration/revocation guard và
  durable arbitration. Desktop dùng PKCE loopback + rotating refresh token trong
  OS keychain; access token không persist và Gateway origin được pin. Deployed
  matrix G1–G4 pass qua `cmd/synce2e/` trên Compose stack (`7708b99`). JetBrains
  3-pane merge UI thuộc **Phase 5 Workbench UI**, không phải blocker Phase 3.
- `ModuleManifest` hiện là contract cho built-in modules với các fields
  `id`, `title`, `version`, `apiVersion`, `commands`, `views`, `capabilities`.
  Backend registry có validation, duplicate protection, deterministic list và
  caller-owned snapshot; `ListModules` chỉ expose public metadata; frontend
  built-in manifests parity-check với backend. Workbench có loading/error,
  compatibility warning, unavailable-module và empty states ở mức tối thiểu.
  `capabilities` chỉ là declaration metadata, grants vẫn do capability
  gateway/policy quản lý.
- External plugin runtime, dynamic loading, và marketplace vẫn ngoài scope của
  Phase 2; sync được triển khai riêng trong Phase 3. MCP runtime + install đã
  Implemented (Resources / Streamable HTTP deferred).
- Vault-item repository is now implemented: một bảng `vault_items` + `internal/vaultitem`
  store, notes/database/ssh là thin adapter, cross-type search qua `VaultSearch`.
  Cross-type search hiện là substring trên metadata JSON và chưa có index trên cột `type`.
- Go packages domain nằm trực tiếp dưới `internal/` (không còn `internal/services/`).
  Sync persistence ở `internal/syncstore`; conflict orchestration ở
  `internal/syncconflict`. Xem [`/dev-kit/repo-structure`](/dev-kit/repo-structure).
- The docs `bun run types:check` command currently reports an unrelated generated
  `.source/server.ts is not a module` error; the production docs build completes
  successfully with TypeScript.
