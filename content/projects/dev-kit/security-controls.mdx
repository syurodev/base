---
title: Security controls
description: Ma trận control implementation và evidence đã kiểm chứng của DevKit.
---

# Security controls

Trang này ghi lại các security control đang có runtime trong checkout desktop `dev-kit-app/`, kèm evidence path và rủi ro còn lại. Kiến thức
threat model, protected assets và security principles nằm ở
[Security model](/dev-kit/security). Boundary contract giữa UI, bridge và gateway
xem thêm [Technical contracts](/dev-kit/technical-contracts); trạng thái module
xem [Implementation status](/dev-kit/implementation-status).

## Control matrix

| Control | Implementation | Evidence | Remaining risk |
| --- | --- | --- | --- |
| Bridge choke point và capability authorization | Mọi built-in operation nhạy cảm đi qua `AppService.call(...)` → `capabilities.Gateway.Call(...)`. Gateway gọi `permissions.Policy.Check` theo `subject/capability/scope` trước khi handler chạy; grant list cố định cho `builtin.*` subjects trong container bootstrap. | `internal/bridge/gateway.go`, `internal/bridge/app_service.go`, `internal/capabilities/gateway.go`, `internal/permissions/policy.go`, `internal/app/container.go`; tests: `internal/capabilities/gateway_test.go`, `internal/bridge/gateway_test.go`, `internal/bridge/app_service_test.go`. | Policy hiện chỉ cover built-in subjects; khi MCP, sync hoặc external plugin runtime xuất hiện cần grant model và scope rules mới. `ListModules()` vẫn là bridge helper không đi qua capability gate. |
| Allowed/denied audit events và metadata redaction | Gateway audit mọi capability call với `OutcomeAllowed` hoặc `OutcomeDenied` trước khi deny hoặc invoke handler. Metadata đi qua `audit.RedactMap` để che key nhạy cảm (`password`, `passphrase`, `secret`, `token`, `recovery`, …) và nested map/slice. | `internal/capabilities/gateway.go`, `internal/audit/events.go`, `internal/audit/redact.go`; tests: `internal/audit/events_test.go`, `internal/audit/redact_test.go`, `internal/bridge/gateway_test.go`, `internal/bridge/app_service_test.go`. | Audit sink hiện là in-memory (`audit.MemorySink`); chưa có persistence, rotation hoặc tamper-evident export cho production forensics. |
| Structured public errors | Bridge map lỗi nội bộ sang `apperrors.AppError` với `code`, `message`, `recoverable`. Validation an toàn, permission denied, vault locked, connection failed và operation cancelled được phân loại; lỗi nội bộ và stack detail không leak ra UI. | `internal/bridge/errors.go`, `internal/apperrors/errors.go`; tests: `internal/bridge/gateway_test.go`. | Một số validation message vẫn pass-through khi nằm trong allowlist; cần review khi thêm module mới để tránh leak implementation detail. |
| Master Password, recovery-key one-time return và vault lifecycle audit | Vault bắt buộc Master Password tại setup (`MinMasterPasswordLength = 8`). Recovery key chỉ trả về một lần từ `SetupVault`, không audit/log/persist. DEK chỉ ở memory khi unlocked; `Lock()` zeroize. Lifecycle events `setup`, `unlock`, `recovery_unlock`, `lock` audit riêng qua `auditVaultLifecycle`. | `internal/vault/service.go`, `internal/bridge/vault.go`, `internal/crypto/keyring.go`; tests: `internal/vault/service_test.go`, `internal/bridge/vault_test.go`. | Length-only password policy; chưa có strength meter hoặc breach-check. Recovery key không rotate tự động sau khi dùng break-glass unlock. |
| SQLite `0600`, `DELETE` journal mode và cleanup khi permission failure | `storage.Open` set `PRAGMA journal_mode=DELETE` để tránh WAL/SHM sidecar không được `chmod`, `chmod` file DB `0600`, `SetMaxOpenConns(1)`. Nếu `chmod` fail thì đóng DB và trả lỗi thay vì để lại file mở với permission lỏng. | `internal/storage/sqlite.go`; tests: `internal/storage/sqlite_test.go` (`TestOpenRestrictsSQLiteFilePermissions`, `TestSQLiteDoesNotCreateUnprotectedWALSidecars`, `TestOpenClosesDatabaseWhenRestrictingPermissionsFails`). | Đây là file permission + application-layer field encryption, không phải SQLCipher/full-database encryption. Note titles và metadata khác vẫn plaintext trong blob JSON. |
| V2 envelope và AAD binding | Chỉ hỗ trợ `EnvelopeVersionV2`; `SealV2` và `OpenWithAD` bind canonical `AssociatedData` (`RecordType`, `RecordID`, `KeyID`) vào AEAD. Envelope V1 bị từ chối với unsupported-version error. | `internal/crypto/envelope.go`, `internal/crypto/aead.go`; tests: `internal/crypto/envelope_test.go`, `internal/crypto/aead_test.go`. | Nếu sau này phát sinh V1 ciphertext ngoài greenfield baseline, cần migration/re-encrypt path được phê duyệt riêng. |
| Desktop OIDC PKCE và refresh-token rotation | `internal/auth` mở system browser cho Authorization Code + PKCE S256, chỉ bind callback loopback cố định `127.0.0.1:62364/callback`, kiểm tra `state`, timeout và không log code/verifier/token. Refresh token được thay thế trong OS keychain trước khi access token mới được dùng; `invalid_grant` xóa credential cũ. Access token chỉ ở memory. | `internal/auth/{service,keyring_store}.go`; tests `internal/auth/service_test.go` cover refresh rotation, rejected refresh và state/PKCE callback. Deployed matrix G4 (`TestG4ExpiredCredentialsClearSession`, `TestG4RevokedDeviceRejected`) pass ngày 2026-08-10 qua `cmd/synce2e/`. | Keychain behavior trên từng OS vẫn cần manual QA; production browser PKCE smoke đã pass nhưng matrix dùng token fixture trên Compose. |
| Sync authenticated transport boundary | Server-authoritative handshake cấp account/device/expiry; HTTP yêu cầu HTTPS ngoài loopback, reject redirect, bound I/O/batch/ciphertext, phân loại `401/403` để clear session. `SyncConnect` pin `https://api.synx.io.vn`; sync lấy access token từ auth service trước mỗi request thay vì nhận bearer từ UI. Companion Gateway MVC validate Keycloak token, strip spoofable identity/proxy headers, ký JWT nội bộ ngắn hạn, và Redis denylist early reject (fail-open); backend enforce account/device/revocation qua PG + self-service revoke API. | Client: `internal/auth`, `internal/sync/{session,httptransport}.go`, `internal/syncclient/{service,connect}.go`, `internal/bridge/sync.go`, `cmd/synce2e/`. Backend: `gateway` denylist filter, `identity` list/revoke (`2060f39`), `bootstrap/security`. `./gradlew :test --tests com.synx.devkit.e2e.SyncApiIT` + `./gradlew :gateway:test` PASS; desktop bridge tests PASS (`02afaaa`). G4 API path updated; re-run `TestG4RevokedDeviceRejected` against Compose when verifying. | Keycloak logout, hardened edge rate limit và key-rotation runbook vẫn later. Device management UI polish deferred. |
| Sync cryptographic context và remote apply | Replication envelope bind account/device/protocol/operation/entity-version/action/key/record vào AAD. Crypto envelope reject sai nonce/tag size trước AEAD. Remote snapshot được validate theo supported record schema; account binding, historical operation journal, monotonic version, tombstone, record apply và cursor update chạy fail-closed trong transaction. Companion backend dùng PostgreSQL unique constraints, advisory lock và atomic transaction cho dedupe/arbitration. | Client: `internal/crypto/{envelope,aead}.go`, `internal/sync/{contract,envelope,queue,transport,runner}.go`, `internal/{syncclient,vaultitem}`, `internal/syncstore/syncremote.go`, `cmd/synce2e/`. Backend: `replication/application`, `replication/adapter/out/persistence`, Liquibase changes `003`/`004`; concurrency/rollback/contract tests. Deployed matrix G1/G3 pass trên Compose stack. | Phase B operational hardening (retention, multi-region) vẫn ngoài scope Phase A. |
| Sync local mutation atomicity và crash recovery | Local create/update/delete commit `vault_items`, account-neutral local entity head và durable intent trong cùng transaction. `Connect` chưa claim account; first `SyncNow` mới bind rồi encrypt intent. Shared snapshot cap ngăn poison-pill; outbox không cho version mới vượt predecessor chưa synced. ACK chỉ clear dirty state khi operation vẫn là head. Pending tombstone chặn remote resurrection. | `internal/vaultitem/sqlmutationrepository.go`, `internal/syncstore/{localmutation,syncoutbox,syncremote}.go`, `internal/syncclient/service.go`, `cmd/synce2e/`; SQL integration/race tests tương ứng. Deployed matrix G1–G3 pass. | Intent snapshot lặp lại plaintext metadata vốn đã có trong local SQLite, nhưng secret vẫn nằm trong inner envelope. JetBrains 3-pane merge UI vẫn deferred UX polish. |
| Bounded Argon2id unlock cost | Persisted KDF params được validate trước `argon2.IDKey`; memory giới hạn 32–256 MiB, iterations 2–6, parallelism 1–8, key/salt length cũng bounded. Default vẫn 64 MiB, 3 iterations, 4 lanes. | `internal/crypto/kdf.go`; tests: `internal/crypto/kdf_test.go`. | Platform-specific tuning/benchmark chưa có; thay đổi bounds hoặc default cần compatibility review với keyring đã persist. |
| SSH exact host-key fingerprint pinning và one-operation passphrase handling | `pinnedHostKeyCallback` chỉ chấp nhận SHA-256 fingerprint đã trust; empty pin luôn reject. `observeHostKeyCallback` dừng handshake trước auth để user approve fingerprint. Passphrase chỉ nhận per-operation từ bridge, `zeroBytes` sau parse; secret/profile decrypt cũng zeroize sau dùng. | `internal/ssh/hostkeys.go`, `internal/ssh/ssh.go`, `internal/bridge/ssh.go`; tests: `internal/ssh/hostkeys_test.go`, `internal/ssh/ssh_test.go`, `internal/bridge/gateway_test.go`. | Host-key trust UX vẫn manual. Passphrase transit qua bridge call metadata nên phụ thuộc redaction + UI không log payload. |
| SSH forwarding loopback boundary và lifecycle cleanup | Local (`-L`) và remote (`-R`) TCP forwarding chỉ bind loopback; forward không persist; stop/profile delete/app shutdown đều đóng listener, SSH client và proxy streams. Forward dùng capability riêng `ssh:forward`; passphrase chỉ dùng trong bước thiết lập client. | `internal/ssh/forwarding.go`, `internal/ssh/ssh.go`, `internal/bridge/ssh.go`, `internal/app/container.go`; tests: `internal/ssh/forwarding_test.go`, `internal/bridge/gateway_test.go`; UI: `frontend/src/modules/ssh/SSHView.tsx`. | Target host/port vẫn do user chọn; chưa có per-destination allowlist hoặc plugin caller surface. |
| Remote DB verified TLS, pool limits, cancellation và row cap | Remote host mặc định `sslmode=verify-full`; `disable` chỉ cho loopback. `PoolManager` giữ một pool/profile với `MaxConns=4`, `MaxConnLifetime=30m`. Query/test dùng `context.WithTimeout` 30s và tôn trọng caller cancellation. `DefaultMaxRows=200` cap kết quả query. | `internal/database/database.go`, `internal/database/pool.go`, `internal/bridge/database.go`; tests: `internal/database/database_test.go`, `internal/database/pool_test.go`, `internal/database/integration_test.go`. | TLS trust phụ thuộc system CA store của pgx; chưa có custom CA pinning. Row cap cố định; arbitrary SQL vẫn chạy được trong giới hạn permission hiện tại. |
| Loopback-only server mode | `servermode.ValidateHost` chỉ chấp nhận blank/default `127.0.0.1`, `localhost`, hoặc loopback IP; reject public/wildcard bind. Desktop/server entrypoint gọi validator trước khi bind Wails server host. | `internal/servermode/servermode.go`, `main.go`; tests: `internal/servermode/servermode_test.go`. | Chỉ enforce tại startup bind address; không thay thế OS firewall hay network policy nếu process bị misconfigure bên ngoài code path này. |

## Evidence (pass 2026-08-10)

Snapshot chuẩn của repo (branch/commit/ngày hiện hành) ở
[Implementation status § Verified snapshot](/dev-kit/implementation-status#verified-snapshot).
Mục dưới chỉ ghi evidence riêng của pass security này, chạy trên
`feat/phase-3-sync-closure` @ `7708b99`: deployed matrix G1–G4 pass qua
`cmd/synce2e/`.

- Verification commands run (representative):
  - `set -a; . .superpowers/sdd/deployed-e2e.env; set +a`
  - `go test ./cmd/synce2e/ -count=1 -timeout 15m`
  - `go test ./internal/arch/ ./internal/sync ./internal/syncclient ./cmd/mocksyncserver/ -count=1`
  - `go vet -tags server ./...`

## Remaining security risks

- **Application-layer SQLite encryption**: Sensitive fields nằm trong crypto envelopes, nhưng SQLite file và nhiều metadata record (ví dụ note title, host/username) vẫn plaintext local. Đây không phải full-database encryption hay SQLCipher.
- **Unlocked-process exposure**: Khi vault unlocked, DEK và plaintext tạm thời nằm trong process memory. Attacker có quyền đọc memory hoặc debug process local vẫn có thể trích xuất secret sau khi unlock.
- **Production evidence đã mở rộng**: deployed matrix G1–G4 pass qua `cmd/synce2e/`
  trên Compose stack (`7708b99`). Self-service revoke API + Redis device denylist
  TTL 60s đã **design-approved** (`2026-08-11-be-phase-b-device-revoke-design`);
  hardened edge rate limit, Keycloak logout và key-rotation runbook vẫn later.
- **Chưa có MCP/external plugin runtime**: MCP (`internal/mcp`), external plugins (`internal/plugins`) và marketplace (`internal/marketplace`) chưa tồn tại. Future surfaces cần threat model và policy mở rộng trước khi ship.
- **Production assumptions chưa được verify end-to-end**: Audit persistence, backup/restore under adversarial disk access, update signing, centralized logging redaction và hardened deployment profile vẫn ngoài scope runtime hiện tại và cần review riêng trước production rollout.
